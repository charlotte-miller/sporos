!!!
%html{ lang:'en'}
  %head
    %meta{charset: "utf-8"}
    %meta{ name:"apple-mobile-web-app-capable", content:"yes"}
    %meta{ name:"viewport", content:"width=device-width, initial-scale=1, maximum-scale=1, minimum-scale=1.0, user-scalable=no, minimal-ui"}
    = csrf_meta_tags
    
    / DNS Prefetch
    %meta{ 'http-equiv'=>'x-dns-prefetch-control', content:'on'}
    %link{ rel:'dns-prefetch', href:'//media.cornerstonesf.chruch'}
    %link{ rel:'dns-prefetch', href:'//direct.cornerstonesf.chruch'}
    %link{ rel:'dns-prefetch', href:'//assets1.cornerstonesf.chruch'}
    %link{ rel:'dns-prefetch', href:'//assets2.cornerstonesf.chruch'}
    %link{ rel:'dns-prefetch', href:'//assets3.cornerstonesf.chruch'}
    
    / Use title if it's in the page YAML frontmatter
    %title= @title || "Cornerstone SF Church"
    
    = stylesheet_link_tag "application", media:'all'
    = yield :page_stylesheets
  
  %body
    = homepage_toggle do
      = render :partial => 'layouts/backlink_tab'
      = yield
    
    #javascripts
      / = partial 'layouts/ie'
      = javascript_include_tag  "vendor", digest:'v1'
      = javascript_include_tag  "application"
      #page_specific_javascripts
        #load_once
          = yield :page_specific_js
        #load_every_time
          = yield :page_specific_js_initializers