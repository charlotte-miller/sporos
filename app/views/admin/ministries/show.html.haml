%p#notice= notice

%h3= "#{@ministry.name.titlecase.possessive} Ministry Dashboard"
%a{href:edit_admin_ministry_path(@ministry)}
  %p
    %b Bio:
    = @ministry.description

- if @posts.empty?
  %h4#no_posts 
    (You have no posts.
    = link_to 'Create One!', new_admin_post_path
    )

- @posts.each do |post|
  = render_partial_for post
