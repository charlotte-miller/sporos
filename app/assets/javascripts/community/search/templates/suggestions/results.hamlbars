{{#if init_help}}
{{> suggestions/_init_help }}
{{/if}}
{{#if empty_help}}
{{> suggestion/_empty_help }}
{{/if}}
%ol.suggestion-list
  {{#each results_collection}}
  %li.suggestion{class:'{{focusClass}}', data:{result_id:'{{id}}'}}
    %i.icon{class:'{{source}}'}
    {{ payload }}
  {{/each}}